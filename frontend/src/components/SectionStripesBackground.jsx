/**
 * Diagonal stripe fill for content sections (not used on hero / CTA pattern sections).
 */
export default function SectionStripesBackground() {
  return <div className="ubuntu-stripes-layer" aria-hidden="true" />;
}
